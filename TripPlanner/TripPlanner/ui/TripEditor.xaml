<Page
    x:Class="TripPlanner.ui.TripEditor"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TripPlanner.ui"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:model="using:TripPlanner.Model"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="{x:Bind Trip.Name, Mode=OneWay}" FontSize="36" />
        <ListView ItemsSource="{x:Bind Trip.ItemList}" Grid.Row="1">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="model:Item">
                    <CheckBox Content="{x:Bind Name, Mode=OneWay}" IsChecked="{x:Bind IsChecked, Mode=TwoWay}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
