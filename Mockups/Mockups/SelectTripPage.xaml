<Page
    x:Class="Mockups.SelectTripPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Mockups"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" Style="{StaticResource DefaultPageStyle}">
    
    <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ListView Grid.Row="1" HorizontalAlignment="Stretch">
            <ListViewItem HorizontalAlignment="Center">Paris, France on 01/1/2016</ListViewItem>
            <ListViewItem HorizontalAlignment="Center">New York, USA on 13/06/2016</ListViewItem>
        </ListView>
        <TextBlock Style="{StaticResource MainTitleStyle}" Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Top"
                   Margin="0 0 0 20">Select a trip</TextBlock>
        <Button Margin="0 10 0 0" Content="Go on a new trip" Grid.Row="2" HorizontalAlignment="Center"/>
    </Grid>
</Page>
